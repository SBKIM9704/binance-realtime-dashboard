# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

두 사용자가 있고, 우선순위는 아래 순서다.

1. **과제 평가자** — 아리닷에이아이 "Binance 실시간 거래 데이터 수집 및 운영 대시보드 구현" 과제의 심사자.
   짧은 시간 안에 화면을 훑으며 요구사항이 실제로 충족됐는지 확인한다. 요구된 지표가 화면에
   **존재하고 찾을 수 있는지**가 곧 평가 결과가 된다.
2. **내부 운영팀** — 수집 파이프라인을 상시 띄워두고 이상을 감지하는 담당자. 데이터가
   끊김 없이·지연 없이·완결성 있게 들어오는지 감시하면서 동시에 시장 현황을 본다.

## Product Purpose

Binance BTCUSDT·ETHUSDT 거래 데이터를 상시 수집하고, **수집 품질과 시장 현황을 같은 화면
체계에서** 보여준다. 성공은 두 질문에 즉답이 되는 것이다 — "지금 데이터가 정상적으로
들어오고 있는가", "지금 시장은 어떤 상태인가".

## Positioning

일반적인 시세 대시보드는 시세만 보여주고 수집 실패를 숨긴다. 이 제품은 **수집 품질을 1급
시민으로 취급한다** — lag·결측/복구율·WS 상태·REST weight를 시세와 나란히 노출하고,
gap 탐지 + REST 백필을 단일 메커니즘으로 처리해 "최초 실행"과 "재시작 누락"을 함께 해결한다.
지표는 대부분 직접 수집·저장한 데이터에서 계산되므로, 수집 품질이 곧 지표 품질이다.

## Operating Context

- 두 프로세스로 동작한다. Collector(상주, WS 수집 + 백필 + reconciler)와 Next.js Web(대시보드)이
  SQLite(WAL) 파일 하나로 연결된다. 수집기는 브라우저와 분리돼 있어 페이지를 몇 개 열든 축적된다.
- 대시보드는 SSE(`/api/stream`)로 1초 주기 스냅샷을 받는다. 스냅샷은 매초 1회만 계산해 전 탭이 공유한다.
- **뷰포트 대응 범위: 높이 약 900px(노트북)~1200px(데스크톱 모니터).** 이 범위 전체에서
  주요 화면은 세로 스크롤 없이 한 화면에 들어와야 한다.

## Capabilities and Constraints

- 수집 인터벌은 기본 1초봉(`KLINE_INTERVAL`)이며 단일 인터벌만 저장한다. 더 큰 봉(5초~1시간)은
  조회 시점에 SQL로 롤업한다(`/api/candles`) — 화면에 인터벌을 추가해도 수집·스키마는 불변.
- `RETENTION_DAYS`(기본 7일) 지난 캔들/이벤트는 자동 정리된다.
- Binance REST weight 예산(6000/분)은 소프트 임계로 자체 페이싱한다.
- 한국어 기본, 영어 전환 가능. 다크 기본, 라이트 전환 가능. 둘 다 쿠키 저장이라 SSR 첫 렌더가 정확하다.
- 임계값 색상 코딩은 확정된 계약이다: Lag(<5s/5–30s/>30s), Recovery(100%/95–99%/<95%),
  Weight(<70%/70–90%/>90%), Error Rate(0/1–5/>5 per min).

## Brand Commitments

- 제품명 **Aria Desk**, 데이터 출처 표기 **Binance**.
- 한국어가 기본 언어이며, 국내 거래소 관례를 따른다 — 캔들 상승은 빨강, 하락은 파랑
  (영어 UI에서는 초록/빨강). 이 관례를 뒤집으면 등락이 반대로 읽힌다.

## Evidence on Hand

- `README.md` — 과제 정의, 기능표, 대시보드 지표 3-tier(System → Pipeline → Market), 임계값 표.
- `docs/metrics.md` — 지표 정의·선택 근거·임계값. `docs/rate-limits.md` — REST 사용량 안전장치.
- 실제 수집된 SQLite 데이터가 `data/market.db`에 존재한다(BTCUSDT·ETHUSDT 1초봉).
- 고객 사례·벤치마크·가격·SLA 같은 주장은 존재하지 않는다. 지어내면 안 된다.

## Product Principles

1. **수집 품질은 시세와 동등한 1급 정보다.** 시세만 예쁘고 파이프라인 상태가 숨겨진 화면은 실패다.
2. **운영자 시선 흐름을 따른다: System → Pipeline → Market.** 정보 구조는 이 순서를 배신하지 않는다.
3. **판단은 색으로 즉시 끝난다.** 숫자를 읽고 해석하기 전에 초록/노랑/빨강이 먼저 답한다.
4. **한 화면에 들어온다.** 주요 화면은 지정 뷰포트 범위에서 스크롤 없이 파악 가능해야 한다.
5. **요구된 지표는 사라지지 않는다.** 화면을 정리할 때 지표를 삭제하지 않고 목적지를 준다.

## Accessibility & Inclusion

- 상태를 색 단독으로 인코딩하지 않는다. 임계 색상에는 항상 라벨·수치가 동반되고,
  캔들 등락은 OHLC 수치와 등락률 텍스트가 함께 표시된다(적녹색약 대비).
- 한/영 전환은 레이아웃을 깨지 않아야 한다 — 한국어가 대체로 더 짧고 영어 라벨이 더 길다.
